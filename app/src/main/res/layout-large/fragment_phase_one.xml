<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:layout_height="wrap_content"
    tools:context="com.juniper.npitracker.fragments.PhaseOneFragment"
   >
<SearchView
    android:id="@+id/phaseone_searchview"
    android:closeIcon="@color/transparent"
    android:focusableInTouchMode="true"
    android:layout_width="match_parent"
    android:focusable="false"
    android:layout_height="wrap_content">

</SearchView>
   <ListView
       android:layout_below="@+id/phaseone_searchview"
       android:id="@+id/phase_one_listview"
       android:layout_width="match_parent"
       android:layout_above="@+id/phase1save"
       android:dividerHeight="0dp"
       android:divider="@null"
       android:layout_height="wrap_content">

   </ListView>

<Button

    android:id="@+id/phase1save"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:visibility="gone"
    android:text="@string/saveprofile"
    android:layout_alignParentBottom="true"
    android:layout_centerHorizontal="true"
    />
</RelativeLayout>
